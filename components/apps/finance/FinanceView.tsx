"use client";

import { useMemo, useState } from "react";
import type { FinanceAccount, FinanceAccountType, FinanceTransaction, FinanceTransactionDirection, FinanceTransactionStatus } from "@/core/contracts/Finance";
import { calculateAccountBalance, calculateClearedBalance, calculateRunningBalances, formatMoney, getCategoryTotals, getMonthTotals, parseMoneyToMinor } from "@/services/finance/domain";
import { useFinanceRepository } from "@/services/finance/localRepository";
import FinanceIntelligence from "@/components/apps/finance/FinanceIntelligence";

const accountTypes: Array<{ value: FinanceAccountType; label: string }> = [
  { value: "checking", label: "Checking" }, { value: "savings", label: "Savings" }, { value: "cash", label: "Cash" }, { value: "credit", label: "Credit card" }, { value: "other", label: "Other" },
];
const today = () => new Date().toISOString().slice(0, 10);
const id = () => crypto.randomUUID();
const inputClass = "mt-1 w-full rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm text-white outline-none transition placeholder:text-white/25 focus:border-cyan-200/45 focus:ring-4 focus:ring-cyan-300/10";
const buttonClass = "inline-flex items-center justify-center rounded-xl border border-white/12 bg-white/[0.07] px-3.5 py-2 text-sm font-medium text-white/78 transition hover:bg-white/[0.13] focus:outline-none focus:ring-4 focus:ring-cyan-300/10 disabled:cursor-not-allowed disabled:opacity-40";

type AccountDraft = { name: string; type: FinanceAccountType; institution: string; startingBalance: string };
type TransactionDraft = { accountId: string; date: string; description: string; merchant: string; amount: string; direction: FinanceTransactionDirection; transferEffect: "in" | "out"; categoryId: string; status: FinanceTransactionStatus; notes: string };

function accountDraft(account?: FinanceAccount): AccountDraft {
  return { name: account?.name ?? "", type: account?.type ?? "checking", institution: account?.institution ?? "", startingBalance: account ? (account.startingBalanceMinor / 100).toFixed(2) : "" };
}
function transactionDraft(transaction: FinanceTransaction | undefined, accountId: string, categoryId: string): TransactionDraft {
  return { accountId: transaction?.accountId ?? accountId, date: transaction?.date ?? today(), description: transaction?.description ?? "", merchant: transaction?.merchant ?? "", amount: transaction ? (transaction.amountMinor / 100).toFixed(2) : "", direction: transaction?.direction ?? "expense", transferEffect: transaction?.transferEffect ?? "out", categoryId: transaction?.categoryId ?? categoryId, status: transaction?.status ?? "cleared", notes: transaction?.notes ?? "" };
}

function Metric({ label, value, hidden }: { label: string; value: number; hidden: boolean }) {
  return <div className="rounded-2xl border border-white/10 bg-white/[0.045] p-4"><p className="text-xs uppercase tracking-[0.18em] text-white/38">{label}</p><p className="mt-2 text-2xl font-semibold tracking-tight text-white/88">{formatMoney(value, hidden)}</p></div>;
}

export default function FinanceView() {
  const { data, ready, saveAccount, saveTransaction, removeTransaction, saveRecurringItem, removeRecurringItem, saveBudget, removeBudget, saveCategory, setSelectedAccount, setHideBalances } = useFinanceRepository();
  const [accountForm, setAccountForm] = useState<AccountDraft | null>(null);
  const [editingAccountId, setEditingAccountId] = useState<string>();
  const [transactionForm, setTransactionForm] = useState<TransactionDraft | null>(null);
  const [editingTransactionId, setEditingTransactionId] = useState<string>();
  const [query, setQuery] = useState("");
  const [categoryFilter, setCategoryFilter] = useState("all");
  const [directionFilter, setDirectionFilter] = useState("all");
  const [statusFilter, setStatusFilter] = useState("all");
  const [formError, setFormError] = useState<string>();

  const activeAccounts = data.accounts.filter((account) => !account.archived);
  const selectedAccount = data.accounts.find((account) => account.id === data.selectedAccountId) ?? activeAccounts[0];
  const balances = useMemo(() => new Map(data.accounts.map((account) => [account.id, calculateAccountBalance(account, data.transactions)])), [data.accounts, data.transactions]);
  const monthly = useMemo(() => getMonthTotals(data.transactions), [data.transactions]);
  const categoryTotals = useMemo(() => getCategoryTotals(data.transactions), [data.transactions]);
  const categoryName = (categoryId: string) => data.categories.find((category) => category.id === categoryId)?.name ?? "Other";
  const selectedRunningBalances = selectedAccount ? calculateRunningBalances(selectedAccount, data.transactions) : new Map<string, number>();
  const transactions = data.transactions.filter((transaction) => {
    if (!selectedAccount || transaction.accountId !== selectedAccount.id) return false;
    const haystack = `${transaction.description} ${transaction.merchant ?? ""}`.toLocaleLowerCase();
    return (!query.trim() || haystack.includes(query.trim().toLocaleLowerCase())) && (categoryFilter === "all" || transaction.categoryId === categoryFilter) && (directionFilter === "all" || transaction.direction === directionFilter) && (statusFilter === "all" || transaction.status === statusFilter);
  }).sort((left, right) => right.date.localeCompare(left.date) || right.createdAt.localeCompare(left.createdAt));
  const totalCash = activeAccounts.filter((account) => account.type !== "credit").reduce((total, account) => total + (balances.get(account.id) ?? 0), 0);
  const checking = activeAccounts.filter((account) => account.type === "checking").reduce((total, account) => total + (balances.get(account.id) ?? 0), 0);
  const savings = activeAccounts.filter((account) => account.type === "savings").reduce((total, account) => total + (balances.get(account.id) ?? 0), 0);
  const credit = activeAccounts.filter((account) => account.type === "credit").reduce((total, account) => total + (balances.get(account.id) ?? 0), 0);
  const defaultCategory = data.categories.find((category) => !category.archived)?.id ?? "";

  const beginAccount = (account?: FinanceAccount) => { setEditingAccountId(account?.id); setAccountForm(accountDraft(account)); setFormError(undefined); };
  const saveAccountForm = () => {
    if (!accountForm?.name.trim()) { setFormError("Give this account a name."); return; }
    const startingBalanceMinor = parseMoneyToMinor(accountForm.startingBalance || "0");
    if (startingBalanceMinor === null) { setFormError("Enter a valid amount with up to two decimal places."); return; }
    const now = new Date().toISOString();
    const existing = editingAccountId ? data.accounts.find((account) => account.id === editingAccountId) : undefined;
    const account: FinanceAccount = { id: existing?.id ?? id(), name: accountForm.name.trim(), type: accountForm.type, ...(accountForm.institution.trim() ? { institution: accountForm.institution.trim() } : {}), startingBalanceMinor, archived: existing?.archived ?? false, createdAt: existing?.createdAt ?? now, updatedAt: now };
    saveAccount(account); setAccountForm(null); setEditingAccountId(undefined); setFormError(undefined);
  };
  const beginTransaction = (transaction?: FinanceTransaction) => { setEditingTransactionId(transaction?.id); setTransactionForm(transactionDraft(transaction, selectedAccount?.id ?? "", defaultCategory)); setFormError(undefined); };
  const saveTransactionForm = () => {
    if (!transactionForm?.accountId || !transactionForm.description.trim()) { setFormError("Choose an account and add a description."); return; }
    const amountMinor = parseMoneyToMinor(transactionForm.amount);
    if (amountMinor === null || amountMinor <= 0) { setFormError("Enter an amount greater than zero."); return; }
    if (!transactionForm.date) { setFormError("Choose a transaction date."); return; }
    const now = new Date().toISOString();
    const existing = editingTransactionId ? data.transactions.find((transaction) => transaction.id === editingTransactionId) : undefined;
    const transaction: FinanceTransaction = { id: existing?.id ?? id(), accountId: transactionForm.accountId, date: transactionForm.date, description: transactionForm.description.trim(), ...(transactionForm.merchant.trim() ? { merchant: transactionForm.merchant.trim() } : {}), amountMinor, direction: transactionForm.direction, ...(transactionForm.direction === "transfer" ? { transferEffect: transactionForm.transferEffect } : {}), categoryId: transactionForm.categoryId || defaultCategory, status: transactionForm.status, ...(transactionForm.notes.trim() ? { notes: transactionForm.notes.trim() } : {}), createdAt: existing?.createdAt ?? now, updatedAt: now };
    saveTransaction(transaction); setSelectedAccount(transaction.accountId); setTransactionForm(null); setEditingTransactionId(undefined); setFormError(undefined);
  };

  if (!ready) return <p className="text-sm text-white/50">Opening local Finance records…</p>;

  return <div className="mx-auto max-w-[1500px] space-y-6">
    <div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-[11px] font-semibold uppercase tracking-[0.24em] text-cyan-100/45">Personal recordkeeping</p><h1 className="mt-2 text-4xl font-black tracking-[-0.04em] sm:text-5xl">Finance</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-white/42">Manual records only. Cosmic does not connect to banks or financial institutions.</p></div><label className="flex items-center gap-3 text-sm text-white/58"><input type="checkbox" checked={data.hideBalances} onChange={(event) => setHideBalances(event.target.checked)} className="h-4 w-4 accent-cyan-300" />Hide balances</label></div>
    <section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5"><Metric label="Total recorded cash" value={totalCash} hidden={data.hideBalances} /><Metric label="Checking" value={checking} hidden={data.hideBalances} /><Metric label="Savings" value={savings} hidden={data.hideBalances} /><Metric label="Credit balance" value={Math.abs(credit)} hidden={data.hideBalances} /><Metric label="Net this month" value={monthly.netMinor} hidden={data.hideBalances} /></section>
    <FinanceIntelligence data={data} selectedAccount={selectedAccount} balances={balances} saveTransaction={saveTransaction} saveRecurringItem={saveRecurringItem} removeRecurringItem={removeRecurringItem} saveBudget={saveBudget} removeBudget={removeBudget} saveCategory={saveCategory} />
    <section className="grid gap-6 xl:grid-cols-[280px_minmax(0,1fr)]">
      <aside className="rounded-3xl border border-white/10 bg-white/[0.045] p-4"><div className="flex items-center justify-between gap-3"><div><h2 className="font-semibold text-white/82">Accounts</h2><p className="mt-1 text-xs text-white/35">Recorded balances</p></div><button type="button" className={buttonClass} onClick={() => beginAccount()}>Add</button></div><div className="mt-4 space-y-2">{activeAccounts.map((account) => <button type="button" key={account.id} onClick={() => setSelectedAccount(account.id)} className={`w-full rounded-2xl border p-3 text-left transition focus:outline-none focus:ring-4 focus:ring-cyan-300/10 ${selectedAccount?.id === account.id ? "border-cyan-200/35 bg-cyan-200/[0.08]" : "border-white/8 bg-black/10 hover:bg-white/[0.06]"}`}><div className="flex items-start justify-between gap-2"><span className="font-medium text-white/78">{account.name}</span><span className="text-xs uppercase tracking-wider text-white/32">{account.type}</span></div><p className="mt-2 text-lg font-semibold text-white/82">{formatMoney(balances.get(account.id) ?? 0, data.hideBalances)}</p>{account.institution ? <p className="mt-1 text-xs text-white/35">{account.institution}</p> : null}</button>)}{!activeAccounts.length ? <div className="rounded-2xl border border-dashed border-white/12 p-4 text-sm leading-6 text-white/45">Add an account to begin keeping a local register.</div> : null}</div>{data.accounts.some((account) => account.archived) ? <p className="mt-4 text-xs text-white/30">{data.accounts.filter((account) => account.archived).length} archived account{data.accounts.filter((account) => account.archived).length === 1 ? "" : "s"}</p> : null}</aside>
      <main className="min-w-0 space-y-6">
        {accountForm ? <section className="rounded-3xl border border-cyan-200/18 bg-cyan-200/[0.045] p-5"><h2 className="text-lg font-semibold text-white/85">{editingAccountId ? "Edit account" : "Add account"}</h2><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm text-white/58">Name<input className={inputClass} value={accountForm.name} onChange={(event) => setAccountForm({ ...accountForm, name: event.target.value })} autoFocus /></label><label className="text-sm text-white/58">Type<select className={inputClass} value={accountForm.type} onChange={(event) => setAccountForm({ ...accountForm, type: event.target.value as FinanceAccountType })}>{accountTypes.map((type) => <option key={type.value} value={type.value}>{type.label}</option>)}</select></label><label className="text-sm text-white/58">Institution <span className="text-white/25">(optional)</span><input className={inputClass} value={accountForm.institution} onChange={(event) => setAccountForm({ ...accountForm, institution: event.target.value })} /></label><label className="text-sm text-white/58">Starting balance<input className={inputClass} inputMode="decimal" placeholder="0.00" value={accountForm.startingBalance} onChange={(event) => setAccountForm({ ...accountForm, startingBalance: event.target.value })} /></label></div>{formError ? <p role="alert" className="mt-3 text-sm text-rose-200/80">{formError}</p> : null}<div className="mt-4 flex flex-wrap gap-2"><button type="button" className={buttonClass} onClick={saveAccountForm}>Save account</button><button type="button" className={buttonClass} onClick={() => { setAccountForm(null); setFormError(undefined); }}>Cancel</button>{editingAccountId ? <button type="button" className={`${buttonClass} text-amber-100/75`} onClick={() => { const account = data.accounts.find((item) => item.id === editingAccountId); if (account) saveAccount({ ...account, archived: !account.archived, updatedAt: new Date().toISOString() }); setAccountForm(null); }}>{data.accounts.find((item) => item.id === editingAccountId)?.archived ? "Unarchive" : "Archive"}</button> : null}</div></section> : null}
        {!selectedAccount ? <section className="rounded-3xl border border-dashed border-white/12 bg-white/[0.03] p-8 text-center"><h2 className="text-xl font-semibold text-white/82">Start with an account</h2><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-white/45">Finance uses only records you enter here. Add a checking, savings, cash, credit, or other account to begin.</p><button type="button" className={`${buttonClass} mt-5`} onClick={() => beginAccount()}>Add account</button></section> : <>
          <section className="grid gap-3 sm:grid-cols-3"><Metric label="Recorded balance" value={balances.get(selectedAccount.id) ?? 0} hidden={data.hideBalances} /><Metric label="Cleared balance" value={calculateClearedBalance(selectedAccount, data.transactions)} hidden={data.hideBalances} /><Metric label="This month spent" value={monthly.expenseMinor} hidden={data.hideBalances} /></section>
          <section className="rounded-3xl border border-white/10 bg-white/[0.045] p-4 sm:p-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h2 className="text-xl font-semibold text-white/85">{selectedAccount.name} register</h2><p className="mt-1 text-xs text-white/35">Recorded balance is derived from the starting balance and saved transactions.</p></div><div className="flex flex-wrap gap-2"><button type="button" className={buttonClass} onClick={() => beginAccount(selectedAccount)}>Edit account</button><button type="button" className={`${buttonClass} border-cyan-200/25 bg-cyan-200/[0.10]`} onClick={() => beginTransaction()}>Add transaction</button></div></div>
            <div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-4"><input aria-label="Search transactions" className={inputClass.replace("mt-1 ", "")} placeholder="Search merchant or description" value={query} onChange={(event) => setQuery(event.target.value)} /><select aria-label="Filter category" className={inputClass.replace("mt-1 ", "")} value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)}><option value="all">All categories</option>{data.categories.filter((category) => !category.archived).map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select><select aria-label="Filter direction" className={inputClass.replace("mt-1 ", "")} value={directionFilter} onChange={(event) => setDirectionFilter(event.target.value)}><option value="all">All activity</option><option value="income">Income</option><option value="expense">Expenses</option><option value="transfer">Transfers</option></select><select aria-label="Filter status" className={inputClass.replace("mt-1 ", "")} value={statusFilter} onChange={(event) => setStatusFilter(event.target.value)}><option value="all">All statuses</option><option value="cleared">Cleared</option><option value="pending">Pending</option></select></div>
            {transactionForm ? <TransactionForm draft={transactionForm} accounts={activeAccounts} categories={data.categories.filter((category) => !category.archived)} error={formError} onChange={setTransactionForm} onSave={saveTransactionForm} onCancel={() => { setTransactionForm(null); setFormError(undefined); }} /> : null}
            <div className="mt-5 overflow-hidden rounded-2xl border border-white/8"><div className="hidden grid-cols-[100px_minmax(0,1fr)_140px_110px_110px_110px] gap-3 bg-black/15 px-4 py-3 text-[11px] uppercase tracking-[0.14em] text-white/35 md:grid"><span>Date</span><span>Description</span><span>Category</span><span>Money in</span><span>Money out</span><span>Balance</span></div><div>{transactions.map((transaction) => <TransactionRow key={transaction.id} transaction={transaction} categoryName={categoryName(transaction.categoryId)} balance={selectedRunningBalances.get(transaction.id) ?? 0} hidden={data.hideBalances} onEdit={() => beginTransaction(transaction)} onDelete={() => window.confirm("Delete this local transaction? This cannot be undone.") && removeTransaction(transaction.id)} />)}{!transactions.length ? <div className="p-7 text-center text-sm text-white/42">{data.transactions.length ? "No transactions match these filters." : "No transactions recorded yet. Add the first transaction to begin the register."}</div> : null}</div></div>
          </section>
          <section className="grid gap-4 lg:grid-cols-[1fr_1fr]"><div className="rounded-3xl border border-white/10 bg-white/[0.045] p-5"><h2 className="font-semibold text-white/82">This month</h2><div className="mt-4 grid grid-cols-3 gap-2"><Metric label="Income" value={monthly.incomeMinor} hidden={data.hideBalances} /><Metric label="Spent" value={monthly.expenseMinor} hidden={data.hideBalances} /><Metric label="Net" value={monthly.netMinor} hidden={data.hideBalances} /></div></div><div className="rounded-3xl border border-white/10 bg-white/[0.045] p-5"><h2 className="font-semibold text-white/82">Top spending categories</h2><div className="mt-3 space-y-2">{[...categoryTotals.entries()].sort((left, right) => right[1] - left[1]).slice(0, 5).map(([categoryId, amount]) => <div key={categoryId} className="flex items-center justify-between gap-3 text-sm"><span className="text-white/55">{categoryName(categoryId)}</span><span className="font-medium text-white/75">{formatMoney(amount, data.hideBalances)}</span></div>)}{!categoryTotals.size ? <p className="text-sm text-white/38">Recorded expenses will appear here.</p> : null}</div></div></section>
        </>}
      </main>
    </section>
  </div>;
}

function TransactionForm({ draft, accounts, categories, error, onChange, onSave, onCancel }: { draft: TransactionDraft; accounts: FinanceAccount[]; categories: Array<{ id: string; name: string }>; error?: string; onChange: (draft: TransactionDraft) => void; onSave: () => void; onCancel: () => void }) {
  const update = <K extends keyof TransactionDraft>(key: K, value: TransactionDraft[K]) => onChange({ ...draft, [key]: value });
  return <div className="mt-5 rounded-2xl border border-cyan-200/15 bg-cyan-200/[0.04] p-4"><h3 className="font-semibold text-white/82">{draft.description ? "Edit transaction" : "Add transaction"}</h3><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><label className="text-xs text-white/52">Account<select className={inputClass} value={draft.accountId} onChange={(event) => update("accountId", event.target.value)}>{accounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</select></label><label className="text-xs text-white/52">Date<input className={inputClass} type="date" value={draft.date} onChange={(event) => update("date", event.target.value)} /></label><label className="text-xs text-white/52">Description<input className={inputClass} value={draft.description} onChange={(event) => update("description", event.target.value)} /></label><label className="text-xs text-white/52">Merchant / payee<input className={inputClass} value={draft.merchant} onChange={(event) => update("merchant", event.target.value)} /></label><label className="text-xs text-white/52">Amount<input className={inputClass} inputMode="decimal" placeholder="0.00" value={draft.amount} onChange={(event) => update("amount", event.target.value)} /></label><label className="text-xs text-white/52">Direction<select className={inputClass} value={draft.direction} onChange={(event) => update("direction", event.target.value as FinanceTransactionDirection)}><option value="expense">Expense</option><option value="income">Income</option><option value="transfer">Transfer</option></select></label>{draft.direction === "transfer" ? <label className="text-xs text-white/52">Transfer effect<select className={inputClass} value={draft.transferEffect} onChange={(event) => update("transferEffect", event.target.value as "in" | "out")}><option value="out">Money out</option><option value="in">Money in</option></select></label> : null}<label className="text-xs text-white/52">Category<select className={inputClass} value={draft.categoryId} onChange={(event) => update("categoryId", event.target.value)}>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="text-xs text-white/52">Status<select className={inputClass} value={draft.status} onChange={(event) => update("status", event.target.value as FinanceTransactionStatus)}><option value="cleared">Cleared</option><option value="pending">Pending</option></select></label><label className="text-xs text-white/52 sm:col-span-2">Notes <span className="text-white/25">(optional, not indexed in Search)</span><input className={inputClass} value={draft.notes} onChange={(event) => update("notes", event.target.value)} /></label></div>{error ? <p role="alert" className="mt-3 text-sm text-rose-200/80">{error}</p> : null}<div className="mt-4 flex flex-wrap gap-2"><button type="button" className={buttonClass} onClick={onSave}>Save transaction</button><button type="button" className={buttonClass} onClick={onCancel}>Cancel</button></div></div>;
}

function TransactionRow({ transaction, categoryName, balance, hidden, onEdit, onDelete }: { transaction: FinanceTransaction; categoryName: string; balance: number; hidden: boolean; onEdit: () => void; onDelete: () => void }) {
  const income = transaction.direction === "income" || (transaction.direction === "transfer" && transaction.transferEffect === "in");
  return <article className="border-b border-white/[0.07] p-4 last:border-0 md:grid md:grid-cols-[100px_minmax(0,1fr)_140px_110px_110px_110px] md:items-center md:gap-3 md:px-4 md:py-3"><div className="text-xs text-white/45">{new Date(`${transaction.date}T12:00:00`).toLocaleDateString(undefined, { month: "short", day: "numeric", year: "numeric" })}</div><div className="mt-2 min-w-0 md:mt-0"><p className="truncate font-medium text-white/78">{transaction.merchant || transaction.description}</p><p className="truncate text-xs text-white/38">{transaction.merchant ? transaction.description : ""}{transaction.merchant ? " · " : ""}{categoryName}</p><div className="mt-1 flex gap-2 text-[11px] uppercase tracking-wider"><span className={transaction.status === "pending" ? "text-amber-200/75" : "text-emerald-200/65"}>{transaction.status}</span><span className="text-white/25">{transaction.direction}</span></div></div><div className="mt-3 text-sm text-white/52 md:mt-0">{categoryName}</div><div className="mt-3 text-sm text-emerald-200/80 md:mt-0">{income ? formatMoney(transaction.amountMinor, hidden) : "—"}</div><div className="mt-1 text-sm text-rose-200/75 md:mt-0">{income ? "—" : formatMoney(transaction.amountMinor, hidden)}</div><div className="mt-3 font-medium text-white/75 md:mt-0">{formatMoney(balance, hidden)}</div><div className="mt-3 flex gap-2 md:col-span-6 md:mt-0"><button type="button" className="text-xs text-cyan-100/65 hover:text-cyan-100 focus:outline-none focus:underline" onClick={onEdit}>Edit</button><button type="button" className="text-xs text-rose-200/60 hover:text-rose-100 focus:outline-none focus:underline" onClick={onDelete}>Delete</button></div></article>;
}
