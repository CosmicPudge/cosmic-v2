"use client";
import type { SettingsRepository } from "@/services/settings/localRepository";
import { defaultAIPermissions, aiPermissionKeys } from "@/core/contracts/AI";
export default function CosmicAIPreferencesPanel({ settings }: { settings: SettingsRepository }) { const ai = settings.data.preferences.ai ?? defaultAIPermissions; return <div className="mt-6"><div className="border-b border-white/[0.08] pb-6"><h2 className="text-2xl font-bold tracking-tight text-white sm:text-3xl">Cosmic AI</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-white/46">Choose which modules the read-only AI layer may consult. Finance, Mail, and Notes are off by default.</p></div><div className="mt-5 space-y-2"><label className="flex items-center justify-between gap-4 rounded-xl border border-white/9 bg-white/[0.03] p-4 text-sm text-white/75">Enable Cosmic AI<input type="checkbox" checked={ai.enabled} onChange={(event) => settings.setPreferences({ ...settings.data.preferences, ai: { ...ai, enabled: event.target.checked } })} className="size-4 accent-cyan-300" /></label>{aiPermissionKeys.map((key) => <label key={key} className="flex items-center justify-between gap-4 rounded-xl border border-white/9 bg-white/[0.03] p-4 text-sm text-white/65"><span>{key === "publicWeb" ? "Public web search" : key[0].toUpperCase() + key.slice(1)}</span><input type="checkbox" checked={ai.modules[key]} onChange={(event) => settings.setPreferences({ ...settings.data.preferences, ai: { ...ai, modules: { ...ai.modules, [key]: event.target.checked } } })} className="size-4 accent-cyan-300" /></label>)}</div><p className="mt-4 text-xs leading-5 text-white/32">Conversations are ephemeral in P1. Provider credentials remain server-only. Cosmic AI has no action tools.</p></div>; }
