"use client";

import type { CosmicPlan } from "@/core/contracts/Entitlements";

export default function DevPlanSwitcher({ accountReady, effectivePlan, override, busy, onSetPlan, onReset }: { accountReady: boolean; effectivePlan?: CosmicPlan; override?: CosmicPlan | null; busy?: boolean; onSetPlan: (plan: CosmicPlan) => void; onReset: () => void }) {
  return <section className="rounded-2xl border border-amber-200/20 bg-amber-200/[0.05] p-4" aria-label="Development plan switcher"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-amber-100/75">Cosmic dev</p><p className="mt-1 text-sm text-white/60">Quickly test the current account’s effective plan.</p></div>{override ? <span className="rounded-full border border-amber-200/30 bg-amber-200/10 px-2.5 py-1 text-xs font-semibold text-amber-100">DEV OVERRIDE ACTIVE</span> : null}</div><div className="mt-4 flex flex-wrap items-center gap-2"><span className="mr-1 text-xs uppercase tracking-widest text-white/40">Plan</span>{(["free", "cosmic_plus"] as CosmicPlan[]).map((plan) => <button key={plan} type="button" disabled={!accountReady || busy} aria-pressed={effectivePlan === plan} onClick={() => onSetPlan(plan)} className={`rounded-xl border px-3 py-2 text-sm transition focus:outline-none focus:ring-2 focus:ring-cyan-200 disabled:cursor-not-allowed disabled:opacity-40 ${effectivePlan === plan ? "border-cyan-200/40 bg-cyan-200/15 text-cyan-50" : "border-white/15 bg-white/[0.04] text-white/65 hover:bg-white/[0.08]"}`}>{plan === "cosmic_plus" ? "Cosmic+" : "Free"}</button>)}<button type="button" disabled={!accountReady || busy || !override} onClick={onReset} className="rounded-xl border border-white/10 px-3 py-2 text-sm text-white/55 transition hover:bg-white/[0.06] focus:outline-none focus:ring-2 focus:ring-cyan-200 disabled:cursor-not-allowed disabled:opacity-40">{busy ? "Updating…" : "Reset override"}</button></div></section>;
}
